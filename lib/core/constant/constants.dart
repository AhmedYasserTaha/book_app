import 'package:flutter/widgets.dart';

const kPrimaryColor = Color(0xff100B20);

const kTransetionDuration = Duration(milliseconds: 300);

const kFeaturedBooks = 'featured_box';
const kNewestBooks = 'newest_box';
